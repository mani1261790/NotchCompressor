# 検証記録

2026-09-07、開発Macで確認。実装の有無と実際に操作確認できた範囲を区別する。

## 自動検証

- Xcode betaのSwift 6.4をSwift 5言語モードで使用。最低OSはmacOS 14。
- ローカルFFmpeg 7.1.1、Apple VideoToolbox。
- 3モードの合成動画変換、全体デコード成功。
- 画質のみの音声パケット、音質のみの映像パケットは全SHA256一致。
- 元ファイルSHA256不変、音声なし、破損、既存名／シンボリックリンクとの衝突、キャンセル停止／一時領域削除を確認。
- キュー順序、最大同時実行数1、失敗後継続、受付時設定、重複、終了停止、再起動復元、破損履歴保持を確認。
- 通常クリック、古いドラッグ情報、ドラッグ終了後の抑制、ノッチなし／あり、負の原点を持つ画面座標、3モード領域を確認。

最終の件数、CI結果はこの文書末尾へ追記する。

## ネイティブ画面で確認したこと

- 生成した `.app` の起動。
- SwiftUIキュー画面の見た目と空状態、元ファイル保持の説明。
- 設定ボタン→設定シート→完了ボタンの操作。
- ffmpeg/ffprobe検出済みと `/opt/homebrew/bin` の実表示。
- ログイン時起動が未有効であること（OSのログイン項目は変更していない）。

## 未確認

- Finderからノッチへドラッグして、左／中央／右の各モードが実際に受付される経路。
- 通常デスクトップ上の展開アニメーション、離脱、Esc、メニューバーとの干渉。
- ノッチあり／なし・複数画面・フルスクリーン・メニューバー自動非表示を組み合わせた実操作。
- 実際の長時間画面収録の可読性・音声・サイズ削減。合成動画の検証は代用にならない。
- ログイン起動の登録・解除と再ログイン。
- 兄のMacでの動作、配布用署名・公証。

Finderのネイティブ自動操作では一時ScreenCaptureKitエラー `-3811`（audio/video capture failure）が発生。その後、アプリの「圧縮した動画を表示」からFinderは再取得できた。自動操作のdragではキュー追加もドラッグ開始の診断ログも確認できず、通常のデスクトップのファイルドラッグを再現した証拠にはできなかった。通常のデスクトップでのドラッグはユーザーへ確認依頼中。ビルドや座標テストをこの確認の代用にせず、Issue #4は実操作条件が確認できるまで開いたままにする。

## 手動確認の手順

1. `.app` を起動。メニューバーから圧縮キューと設定が開けることを確認。
2. テスト用のMOV/MP4をFinderで選び、通常のマウス移動だけではパネルが開かないことを確認。
3. ファイルを上端中央へドラッグし、ノッチにつながって展開することを確認。
4. 左・中央・右へ別々にドロップし、キューのモードと出力が一致することを確認。
5. 離脱・Escではキューに追加されず閉じることを確認。
6. 複数ファイル、音声なしの音質のみ、処理中キャンセル、終了して継続／中止を確認。
7. 元ファイルが残り、出力をFinderで開いて再生できることを確認。

成功・失敗、使った画面、対象ファイルの音声有無をIssueへ記録する。未確認の項目をチェック済みにしない。

## 最新の回帰結果

`NOTCH_MEDIA_TESTS=1 bash scripts/test.sh`：22件、失敗0、スキップ0（実メディア4件を含む）。Unicodeの長い名前、排他的rename、既存シンボリックリンク衝突も成功。ローカルreleaseビルドとアドホック署名検証に成功。

## 追加のネイティブ確認

- キューの「動画を選ぶ…」→「画質＋音質」→NSOpenPanelのパス指定→Openから、実際の変換を開始。
- 合成8秒動画: 完了、6.2 MB→1.8 MB、71%削減をSwiftUI画面で確認。出力のffprobe duration=8.000000、size=1799602。
- 「圧縮した動画を表示」でFinderに正しい出力を選択表示。
- 合成120秒動画: 93.2 MB→26.7 MB、71%削減。再起動後も完了履歴を復元。
- 合成1200秒動画: ⌘Qで確認ダイアログ→「処理を続ける」で7%の処理へ復帰→再度⌘Q→「中止して終了」→13%で停止中表示→終了。
- 停止後、履歴がcancelled、元動画932121857 bytesが存在、ジョブの一時ディレクトリなし、確定出力なし、FFmpeg子プロセスなしを確認。
- テストで使用した動画はすべて `.build/manual-fixtures` 内に生成。利用者の既存収録は使用していない。

この終了確認は⌘Qの実経路の証拠。未操作のメニューバー終了やOSログアウトを同等の確認済みとして扱わない。

## CI

[初回CI](https://github.com/mani1261790/NotchCompressor/actions/runs/34104957251)（fe74ba6）は成功。macos-15で単体テスト、release app生成、codesign検証を実行。ハードウェア依存の実メディア4件はCIではスキップし、開発Macで別途検証。

追加の環境別確認はIssue #6、実収録での品質評価はIssue #7、配布版はIssue #8。基本のノッチ実ドラッグは引き続きIssue #4で追跡する。

最終のアプリ再起動はMacロックにより実施できず。ロック解除後の通常デスクトップ確認が必要。94eb636のCI（run 34106097827）も成功。その後、checkoutを公式v7.0.1のSHAに固定してNode 24へ更新した。


## 2026-09-07 18:50 続行時の状態

Macのロックは解除済み。修正版のreleaseアプリを起動し、履歴とキュー画面を取得できた。ドラッグのpasteboard世代変更時にURLがまだ提供されていないと、その後URLが読めても検出できない状態遷移を修正。同じマウス押下中のみ遅延URLを受け付け、終了後の古いpasteboardでは起動しない。`bash scripts/test.sh --filter NotchTests` は4件成功。releaseビルド成功。

FinderでNotch-Test.movを選択表示するまで成功したが、スクリーンショット取得はScreenCaptureKit -3811で失敗した。この試行ではドラッグ操作に到達していない。ノッチ表示とFinderドロップ→キューは引き続き未確認。修正した状態遷移が実機での表示問題の原因だったとは断定しない。Issue #4 / Goal #4は未完了。


## 2026-09-07 通常時は縮小表示する仕様へ修正

ユーザーの訂正に従い、通常非表示を廃止。常時ノッチと重なる黒い縮小パネルを表示し、ファイル接近時のみ3領域へ展開、終了時は縮小する。NSScreenの左右補助領域からノッチ幅を計算し、ノッチなしでは180×28pt。ウインドウ自体を縮小するため大きな透明領域は残らない。ファイルのnative draggingEntered/Updatedからの展開経路も追加。

NotchTests 5件成功、releaseビルド成功、修正版起動済み。起動後のOSウインドウ一覧で、画面上端X=870,Y=0にWidth=180,Height=28,layer=25のアプリパネルが存在することを確認。これは縮小パネルの配置確認であり、Finderからの実ドラッグ→展開→キューの証拠ではない。後者は未確認のまま。


## 2026-09-07 19:15 ドロップ受付の安定化

NativeDropSessionを追加。AppKitからドラッグ進入を受けた間は、全体ポーリングによる縮小を抑止し、マウスアップからドロップ通知まで対象領域を維持する。終了・離脱通知で解除し、通知が来ない場合はボタンを離して350msで縮小へ戻す。開始・リリース・終了・キャンセルの単体テスト追加。状態変化のみをOSLogへ記録し、ファイル名は記録しない。

実FFmpegを含む25テスト成功（スキップ0）、releaseビルド成功、修正版起動済み。Computer Useのアプリスクリーンショットで黒い180×28の縮小表示を実際に確認。19:11:51のOSLogに実ファイルドラッグのpasteboard変更（leftDown=true,files=true）が記録された。ただしFinderからのドロップ→キュー成功はまだ確認できていない。自動dragはユーザーによるFinderウインドウ変更を検出して実行前に止まった。Finderを再取得するとDesktopが選ばれる。確認できていない操作を合格としない。


## 2026-09-07 キューと設定のUI刷新

AppleのLiquid Glass導入ガイドとSwiftUIのglass/glassEffect APIに合わせ、macOS 26以降では標準Glassボタンを採用。macOS 14/15では標準の丸いボタンとMaterialにフォールバックする。追加・設定・元ファイル・再試行・キャンセル・履歴消去は説明付きアイコンにし、結果表示・フォルダ選択はピル型。キューは静かな背景のカード、設定はgrouped Form、標準segmented pickerとswitch。ガラスは操作部品に限定する。

releaseビルド成功。起動したキューのスクリーンショットで追加ボタンの過剰な横幅を見つけて修正し、丸い追加・設定とピル型結果ボタンを再確認。設定シートの全セクション、ツール検出、設定を閉じる操作を実画面で確認。追加アイコンから3モードのメニューが開くことも確認。ログイン設定の変更や新たな変換は実行していない。ノッチ実ドロップ検証は別途未完了。

参照: https://developer.apple.com/documentation/technologyoverviews/adopting-liquid-glass
参照: https://developer.apple.com/documentation/swiftui/applying-liquid-glass-to-custom-views


## 2026-09-07 円形操作と動画サムネイル

ユーザー訂正: アイコンのみは円、文字付き横長ボタンはピル。IconControlを標準glass + circleに変更し、追加メニューも44ptの円へ統一。macOS 14/15はCircle + Material。大きな圧縮モード記号を廃止し、Quick Lookで各動画のサムネイルを非同期取得。完了時は出力を優先し、なければ元動画。未ダウンロードのiCloudファイルはサムネイル生成をスキップ。失敗時は形式付きプレースホルダー、画面から消えたリクエストはキャンセル。圧縮モードはファイル名下の補足ラベル。

releaseビルド成功。起動した実画面で円形ボタン、ピル型の結果ボタン、個別動画サムネイルを確認。追加ボタンだけ小さくなる問題を再調整し、最終スクリーンショットで設定ボタンと揃う円を確認。検証動画は同じカラーバー素材なのでサムネイルが似ているが、各URLをQuick Lookに渡している。大元のFinder→ノッチドロップ検証はこのUI変更の検証に含めない。


## 2026-09-07 ビルド環境の修正

サムネイルUI追加後のCI 34111319040は失敗。macos-15の既定Xcode 16.4にはLiquid Glass SDKがなく、#availableだけでは未定義APIをコンパイルできないことが原因。GitHub公式runner-imagesのインストール済み一覧を確認し、CIにXcode 26.3を明示。修正後run 34111533113は単体テストまで成功、releaseを実行中。

build-app.sh/test.sh共通のselect-xcode.shを追加。明示したDEVELOPER_DIRを尊重し、それが無い場合のみインストール済みのSDK26以上かつフルXcodeを選ぶ。システム設定は変えない。対応SDKが無い・明示パスが不正なら必要バージョンと指定方法を出して停止。通常のbash scripts/build-app.shでビルド成功、不正な明示パスが案内付きexit1となることを確認。

基本Finderドラッグは引き続き未確認。このターンでもドラッグ呼び出しはFinder状態変更エラーで止まり、観察用pasteboardはcount117/filesfalseのまま。新しいキュー追加・展開を示す証拠なし。


## 2026-09-07 保存仕様の変更（旧記録より優先）

ユーザーが成功時の上書きを明示指定。従来の別名保存を廃止し、元と同じ名前・場所に置き換える。一時ファイルで変換・再解析・全デコード検証を完了し、元のサイズ/mtime/inodeを直前確認してFileManager.replaceItemAtを実行。既存ファイル権限を維持。MOVはMOV、MP4/M4VはMP4コンテナで書き出す。失敗・キャンセル・外部変更検出なら元データを維持。成功後の圧縮前データは残さない。サイズ増加時も置き換え、実測増加を表示する。

実FFmpegを含む26テスト、失敗0/スキップ0。3モードで同名出力・変更側codec・保持側packet hash一致、MP4/M4Vのftyp、Unicode名と権限、外部変更拒否、置き換え失敗時保持、キャンセル時元バイト列一致を確認。合成した一時ファイルだけを使用し、既存のユーザー動画には実行していない。旧履歴に残る別名出力は移動/削除しない。ノッチ実ドラッグ検証はこの変更の成功証拠ではない。


## 2026-09-07 Mission Control干渉への対策

ユーザー提供の10.2秒録画を確認。ファイルドラッグでノッチ展開と3領域の強調は実際に動き、その後Mission Controlが開いている。19:56:21.440にexpanded、19:56:21.806にnative destinationが記録された。従来の「展開すら未確認」はこの録画で更新。ただし今回録画はドロップ完了の証拠ではない。

パネルをstationary/ignoresCycle/canJoinAllApplicationsとしてMission Controlの整列から除外し、isMovableと背景移動を禁止。ドラッグ中に受付矩形が移動するNSWindowのフレームアニメーションを停止。接近判定を上端から120pt（ノッチ高32ptでも120pt）まで広げる。screenIndexとtrigger判定に外側境界を含め、CGRectのmaxY境界で判定を失わないよう修正。通常のマウス移動では展開しない条件は維持。OSのMission Control設定は変更していない。

NotchTests 7件成功、releaseビルドと修正版起動成功。Mission Controlが同じ操作で再発しないことは未検証で、完了を断言しない。提供動画自体は読み取りのみ。展開の証拠と干渉解消の証拠を区別する。

参照: https://developer.apple.com/documentation/appkit/nswindow/collectionbehavior-swift.struct

## 2026-09-07 FFmpeg同梱と配布準備

公式FFmpeg 9.0.1 tar.xzを上流PGP鍵（FCF986EA15E6E293A5644F10B4322F04D67658D8）で検証。チェックサム固定、ソース無改変、GPL/nonfree/network/autodetect無効でarm64ビルド。FFmpeg自身の-L表示はLGPL 2.1以降。otoolではmacOSシステムライブラリ・フレームワークのみ。通常アプリは同梱ツール専用、外部設定UIは撤去。

同梱した実行ファイルを指定し27テスト成功、スキップ0。アドホック署名のdeep/strict検証成功。ネイティブ起動と設定画面のバージョン・Apache/LGPL表示を確認。ユーザーの既存収録にはテスト実行していない。

Git全履歴114 blobに典型的な秘密鍵/認証トークンのパターンなし。追跡されたユーザー動画・画像なし。Issuesと13コメントを点検、ユーザー添付録画のアップロードなし。これは包括的な秘密情報不存在の保証ではない。

Developer IDはユーザー指示で保留。Mission Control修正後のFinderドラッグ、追加実機、代表収録品質、ダウンロード後のGatekeeperは引き続き未確認。Releaseは公開前の下書きとして準備する。

## 2026-09-07 自動スケジューラ（#10）

自動/直列/最大2件の実行方式、待機優先度・次に処理・新規開始の一時停止を追加。既定の自動は同時2件以内・映像変換1件以内。省電力/温度/マシン構成を5秒間隔およびジョブ開始時に評価する。上限変更時も実行中は継続。別名の同一元ファイルをinode+volumeとパスで排他し、キャンセル中もworker終了まで枠を保持する。

同梱FFmpegで35テスト成功（スキップ0）。実FFmpegの2件同時実行と追加投入→同名置き換え、混在モードの空き枠利用、FIFO/優先度/先頭送り、pause/resume、環境変化時のdrain、リンク排他、並列失敗後の継続、全worker終了、旧形式の復元を確認。

実アプリでは、新しい実行方式・一時停止の表示、停止中にファイル選択で合成3動画の一括追加→待機3件、各行の優先度/次に処理ボタンの表示を確認。ノッチからの複数ドラッグ完了とは別の証拠。確認途中にMacがロックされ、ツールは自動解除不能を報告。優先度変更→再開→完了の実操作と、ピル型Menuの最終表示確認は解除後に残る。一般公開や実機合格として扱わない。

### 待機順をドラッグで変更する仕様へ更新

ユーザー指定により、高/標準/低の優先度と「次に処理」を撤去。待機中カードのドロップ時だけ配列順を変更・保存し、その順序をスケジューラが使用する。実行中・終了済み・無効なIDへの移動は拒否。旧priorityフィールドは読み飛ばす。ドラッグ開始だけでは順序を変更しない。

36テスト成功、スキップ0。上下への移動、実行中/終了済みの拒否、並び順の保存、旧履歴互換性を含む。実アプリでは優先度メニューの撤去、ドラッグ案内、ピル型実行方式、3件追加、一時停止/再開から全件完了を確認。メニューバーの「圧縮キューを開く」からキューが開くことも確認。

自動操作ではドラッグ開始を確認したが、ドロップ完了は未確認。専用UTTypeのInfo.plist登録とdata準拠、即時ペイロードを追加。QueueReorderログはファイル名やデータを含めず、開始・ドロップ可否・確定のみを記録する。順序変更後の実行という最終経路は引き続き未確認で、UIテストや旧バージョンのCI成功を代用しない。

## 2026-09-07 手操作の並べ替え確認と完了カード自動除外

21:15の手操作でQueueReorderのvalidate supported=true/source=true、drop entered、drop committedを確認。その後の実アプリで待機3件の再開、全件の検証済み上書き完了を確認した。ドラッグ並べ替えの実経路の確認待ちは解消。自動操作では届かなかったイベントを、実際の操作ログで区別して確認した。

追加指定により、成功したカードは完了の都度表示中のキューから外す。内部の上限付き履歴と動画本体は保持。失敗/キャンセル/中断は確認と再試行のため残す。空状態と終了項目のクリア操作も表示対象を基準にした。

37テスト成功、スキップ0。並列実行の片方が先に成功した時点で当該カードのみ消え、残りの実行は継続すること、キャンセルが残ること、復元済みの成功カードが表示されないことを確認。更新版のネイティブ起動で、従来の成功カードがすべて消え、キャンセル2件だけが残ることを確認した。

## 2026-09-07 実行中の固定・個別停止と除外（#12）

40テスト成功、スキップ0（同梱FFmpeg使用）。実行中/停止待ちの並べ替え・除外拒否、バックフィル後も既存実行カードが先頭、個別停止が別workerに波及しないこと、全体停止の終了待ち中に次の処理が開始しないこと、停止後の並べ替え/除外、除外の永続化と元ファイル保持を確認。復元テストの2ジョブが同じUUIDを共有していたfixtureも修正。

releaseビルド成功。実アプリで180秒の合成動画Stop-Test.movを追加→再開→圧縮中に個別停止→停止後にそのカードだけを除外。実行中は先頭固定表示と停止ボタン、停止後は並べ替え案内・再試行・除外ボタンに変化し、既存のキャンセル2件が残ることを確認。元ファイルSHA256は前後とも99ea166ca2c497890c82624bb2cb83708bfc985c340a7c827289ce483b1e9654。全体停止の複数worker連携は自動テストで確認した。今回、物理ドラッグの再検証は行っていない（前節の手操作で確認済み）。

## 停止カードの上部保持（#13）
停止終了時に元の投入位置へ戻るため、画面外に消えたように見えていた。停止カードを非実行カードの先頭へ移して保存する。他の実行中カードはその上に固定。手動の並べ替え・除外は可能。自動履歴整理を成功済み100件だけに限定し、停止/失敗/中断は自動削除しない。41テスト成功（同梱FFmpeg、スキップ0）。保存/復元と105件の追加成功後の保持を確認。実アプリでも合成Stop-Test.movを停止し、既存の停止カードより上・先頭に残ることを確認した。検証後はこの合成カードだけ手動除外。

## 2026-09-07 一時停止からの途中再開（#14）

45テスト成功（同梱FFmpeg、スキップ0）。実プロセスの一時停止中の出力固定、同じPIDでの続行・出力連続性、繰り返しpauseの釣り合わせ、停止時間をtimeoutに加算しないこと、停止したプロセスのキャンセル終了、置き換え確定との競合を確認。キューの実行枠保持、他のworkerへの非波及、途中再開でOperationを再呼び出さないこと、停止中の除外がworker終了を待つこと、保存復元でinterruptedになることも検証。

実アプリでは180秒の合成Stop-Test.movを20%で一時停止。FFmpeg PID 99243がT状態、CPU累積3.24秒で停止し、元ファイルSHA256は保持。UIの20%が固定されたまま。▶で21%から進行し、同じPIDがR状態・CPU6.10秒へ進み、その後検証・正常完了してカードが消えることを確認した。さらに同じ合成動画を再投入し、全体一時停止（17%）→×で個別除外を確認。残存FFmpegなし、一時出力ディレクトリなし、除外前後の元動画SHA256は2c56d7f0d9541ba21eb874bc6897a7273854da30d5213bfb9dc3894cea76ac13で一致。

途中から再開できるのはアプリを開いたままの間。アプリ終了・Mac再起動後をまたぐ永続チェックポイントではない。UIとREADMEと終了ダイアログに明記。同じ元ファイルの排他と最大2workerの枠は一時停止中も保持する。

API根拠: [Apple Process.suspend](https://developer.apple.com/documentation/foundation/process/suspend()) / [resume](https://developer.apple.com/documentation/foundation/process/resume())。suspend/resumeを一対にし、除外時は再開してから終了シグナルを送る。

## 全体停止操作の撤去（#15）
全体一時停止/全体再開/新規開始だけの一時停止をキュー上部から撤去。個別カードの一時停止/途中再開/除外は変更なし。45テスト成功（同梱FFmpeg、スキップ0）、releaseビルドとパッケージ検証成功。新版を起動し、キュー上部に実行方式だけが残り全体停止系の操作がないことを実アプリのAXで確認。

## カスタム圧縮設定（#16）
49テスト成功（スキップ0、同梱FFmpeg）。カスタム50%/80 kbpsが8 Mbps映像に対して4 Mbps/80 kbpsの引数になること、変更しないトラックはcopy、値の境界、音声の元bitrate上限、映像bitrate欠落時の推定、旧設定JSON互換、設定保存/ジョブごとの投入時スナップショットを確認。実FFmpegでカスタム変換→出力検証→同名置換を確認。実アプリでカスタム選択、映像65→64%・音声96→88 kbpsの操作と表示、スクロール可能な設定画面を確認。検証値を65%/96 kbpsへ戻し、元の標準プリセットを選択した状態で終了。

## 品質設定と変換方式の説明（#17）
文言と表示のみ更新。既存の圧縮アルゴリズムや保存rawValueは変更していない。releaseビルド・パッケージ署名検証成功。実アプリで標準→品質優先の切り替えによって映像65%/音声96 kbpsから85%/128 kbpsの説明に変わること、スクロールして圧縮対象の3通りとHEVC/AAC・解像度/fps・copyの説明が読めることをAXとスクリーンショットで確認。元の標準に戻した。テスト追加・回帰テスト再実行は文言変更のみのため行っていない。
