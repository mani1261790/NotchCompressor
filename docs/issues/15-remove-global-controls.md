## 仕様

全体一時停止・全体再開・新規開始だけの一時停止をキュー画面から撤去する。個別カードの一時停止・途中再開・除外は保持する。実行方式の選択も保持。全体停止状態は永続化していないため、新版起動時は通常の自動開始状態になる。

個別一時停止中はそのworker枠と入力排他を保持する既存仕様を維持する。

## 確認

既存回帰テストとビルド、更新版の実アプリで全体操作がなくなったことを確認する。
