## 仕様

設定にカスタムを追加。映像は元ビットレートの10〜100%（1%刻み）、音声は32〜256 kbps（8 kbps刻み）。既存3プリセットは維持。映像ビットレート不明時は容量/時間から推定。音声は元のビットレートを超えない。出力サイズの削減率保証ではないことを明示。

自動保存し次の投入から適用、投入済みジョブは設定スナップショットを保持。キューカードには適用した数値を表示。旧設定JSONは従来の動作を維持。

検証: 数値とFFmpeg引数、保持モード、値の境界、旧設定復元、新規投入への適用、実アプリ設定。
