## 目的

複数・連続ドロップで受け付けた動画について、安全に実行数と待機順を調整し、キュー画面から操作できるようにする。

## 実装方針

- 複数URLの一括投入・処理中の追加入力を維持。
- 自動は最大2件、映像圧縮は同時1件まで。音声のみは空き枠へ割り当てる。省電力・発熱時、メモリ8GB未満/4論理CPU未満なら新規起動を1件までに制限。
- 最適化はリソース状況に応じた保守的なルール。速度の実測学習や常に最速を保証するものではない。
- 手動で1件ずつ/最大2件を選択。実行方式を変えても処理中を打ち切らず、以降の起動を調整。
- 待機カードのドラッグ＆ドロップで並べ替え。一時停止は現在の処理を継続。高/標準/低の優先度設定や先頭送りボタンは撤去。新規追加は末尾。
- パス別名・シンボリックリンク・ハードリンクも同一ファイルなら同時処理しない。
- 表示は実行中→実際の待機順→履歴。キャンセル中も停止完了まで実行枠を保持。
- 旧履歴との互換性、並び順/実行方式の保存、終了時の全worker停止を維持。

## 完了条件

- [x] スケジューラと互換保存
- [x] キューのドラッグ並べ替えUI
- [x] 並列上限、待機順、連続投入、同一入力排他、失敗/キャンセル/停止、環境変化のテスト
- [x] 同梱FFmpeg回帰、実アプリの表示/操作確認
