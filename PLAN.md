# NotchCompressor 実装計画

## 目的と初期リリースの範囲

Finderから画面収録を上端中央へドラッグし、左＝画質のみ、中央＝画質と音質、右＝音質のみの領域へドロップすると、バックグラウンドで圧縮する。SwiftUIを表示層、AppKitをウインドウ・ドラッグ・アプリ終了制御に用いる。macOS 14以上を対象とする。

通常のマウス移動やクリックではノッチUIを開かない。ノッチの物理的なカメラ領域を操作必須にせず、中央ゾーンを直下に確保する。処理中の大きなパネル常時表示はせず、メニューバーからキューと結果を開く。

## 決定事項

- エンジンはローカルのFFmpeg / ffprobe。外部サーバーへ動画を送らない。
- 選択されなかった側のストリームは `copy` で保持する。互換性エラーで勝手に別モードへ切り替えない。
- 保存先は元ファイルと同じフォルダ。MOVとして別名保存し、既存ファイルと元ファイルを上書きしない。
- 元ファイルは常に保持する。空き容量を増やすには、結果を確認した後に利用者が元ファイルを整理する。
- 出力が元より大きくなる場合もあり、実測の増減を表示する。削減を保証しない。
- 複数ファイルは直列処理。設定は受付時にスナップショットし、実行中に変更しない。
- 中断したジョブは次回起動時に中断として復元し、自動再実行しない。
- 別Macへのインストールや既存動画の変換は、この実装作業では行わない。

## 入力と圧縮ルール

初期対応はローカルにあるMOV / MP4 / M4V、動画1トラック、音声0〜1トラック。添付画像、字幕、多重映像、多重音声などを黙って捨てず、非対応として理由を示す。iCloud未ダウンロードはFinderでダウンロードする案内を出す。フォルダ・読めないファイル・壊れたメディアはジョブ単位で失敗させ、次のジョブへ進む。

| モード | 映像 | 音声 |
| --- | --- | --- |
| 画質のみ | HEVC VideoToolboxで再エンコード | copy |
| 画質＋音質 | HEVC VideoToolboxで再エンコード | AACで再エンコード |
| 音質のみ | copy | AACで再エンコード |

映像変換はまずSDR・8bit・不透明映像に限定する。HDR、透過、高ビット深度は意図せぬ色変換を避けるため説明して拒否する。音声のみモードでは映像をcopyするので、この制限を適用しない。音声変換はモノラル／ステレオ。無音声ファイルの音質のみモードは変換前に拒否し、両方モードでは映像のみ処理することを明示する。

映像は解像度を維持し、30fpsを上限にする。3強度（画質優先／標準／容量優先）を設定に用意し、画素数・fps・既知の元ビットレートから目標を決める。音声は128 / 96 / 64kbps。既定は標準。数値は暫定で、実際の収録での可読性・音声評価は別途必要。

## 層と責務

- **Core / MediaProbe**：ffprobe JSONを解析し、形式・トラック・時間・サイズ・色・音声有無を検証。
- **Core / CompressionPolicy**：強度とモードを純粋な引数列へ変換。シェルを介さない。
- **Core / ProcessRunner**：非同期実行、進捗、ログ、タイムアウト、キャンセル。出力をファイルへ流してパイプ詰まりを避ける。
- **Core / CompressionEngine**：入力検証→領域確保→変換→出力再解析→全体デコード検査→変更検知→競合しない確定保存。
- **App / QueueStore**：待機・解析・変換・検証・完了・失敗・キャンセル。設定と結果を保存。再起動時は未完了を中断に変換。
- **App / NotchPanel**：ドラッグの開始／離脱／キャンセル、画面変更、3領域の強調と受付。
- **App / QueueWindow & Settings**：進捗、個別キャンセル・再試行、Finder表示、依存ツール選択、強度、ログイン時起動。

## 安全な変換

1. 入力URLの実在、通常ファイル、読み取り、クラウド実体、形式を検査。
2. ffmpeg/ffprobeの実行可能性を検査。未設定時は設定への直接導線を表示。
3. 出力先の書き込みと空き容量を検査。容量推定は保証ではなく、変換中のENOSPCも失敗として扱う。
4. 元ファイルのサイズと更新日時を記録。ジョブ固有の隠し一時ディレクトリを保存先へ作る。
5. `-nostdin` / `-n`、明示的なmap、選択モードのcodecで変換。進捗は `-progress` から得る。
6. 出力の映像・音声数、長さ、サイズ、copy側codecを検証。全体を `-xerror` でデコードする。
7. 元のサイズ・更新日時が変わっていたら成功にしない。
8. 衝突したら連番の名前を選び、置き換えなしで確定する。成功・失敗・キャンセル時に当該ジョブの一時出力だけを片付ける。

アプリ終了時に進行中／待機中があれば、終了してキャンセルするか継続するかを確認する。異常終了時は完成ファイルとして扱わず、一時ファイルが残る可能性を結果とドキュメントで説明する。自動削除はしない。

## IssueとGoalの順序

各Issueの開始時に専用Goalを作成。受け入れ条件を満たした後にGoalをcompleteにし、検証結果をIssueへ記録して閉じる。次のIssueのGoalを作る。達成していない条件を完了扱いにしない。

1. 入力解析・依存検出・3モードのルールと単体テスト。
2. 変換・進捗・検証・キャンセル・安全保存と実メディア統合テスト。
3. 直列キュー・復元・設定・結果UI・終了制御。
4. ノッチの連続した形状・ドラッグ受付・画面追従と実際の操作確認。
5. ローカルアプリ生成・CI・回帰検証・利用手順・検証記録。

## 検証の区別

- 単体：ポリシー、解析、保存名、キュー復元、ドラッグ状態・画面座標。
- 統合：合成動画の3モード、copy側のパケットハッシュ一致、音声なし、破損、キャンセル、ファイル名衝突、元データ不変。
- 実機：Finderの実ドラッグ→選択→変換→結果、普通の移動で非表示、離脱・Esc、キュー操作、終了確認。
- 環境別：ノッチ画面、ノッチなし、複数画面、フルスクリーン、メニューバー自動非表示。利用できない環境は未検証と記録し、必要なら追跡Issueへ分ける。
- 配布：ローカルアドホック署名を確認。Developer ID署名・公証・FFmpeg同梱・自動更新は初期版の範囲外。FFmpegは同じMacへの別途導入が必要。

## 参照

- https://ffmpeg.org/ffmpeg.html （streamcopy、map、progress）
- https://ffmpeg.org/ffprobe.html （JSON解析）
- https://developer.apple.com/documentation/appkit/nsscreen/safeareainsets （カメラ領域と安全な表示位置）

2026-09-07: 初期プロトタイプを確認して計画を具体化。実装の進捗と検証記録はCONTEXT.mdと各Issueに記載する。

## 後続の検証・配布Issues

- #6: 追加の画面構成とログイン時起動の実機検証。
- #7: 利用者が指定する実収録での品質・容量評価とプリセット調整。
- #8: 配布版のFFmpegセットアップ、アーキテクチャ、署名・公証。

これらは初期実装の確認範囲外を明示する追跡Issue。#4の基本的なFinder→ノッチ操作を後続Issueへ移して完了扱いにはしない。
