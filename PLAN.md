# NotchCompressor 初期仕様案

画面収録ファイルをノッチ付近へドラッグし、ドロップ位置だけで圧縮方法を選べるmacOSアプリ。設定画面を毎回開かずに、溜まった収録を整理できることを目指す。

## 確定している要望

- ファイルをドラッグしてノッチ周辺へ近づけたときだけ、ノッチ周辺が広がる。
- 左は画質のみを落とす。右は音質のみを落とす。中央は両方を落とす。
- ドロップした位置によって圧縮方法を決める。
- 添付のNotchBox画像は、上端につながる形状と拡張時の見え方の参考とする。

## 提案する操作

1. 通常時はパネルを隠す。通常のマウス移動では開かない。
2. ファイルのドラッグが画面上端中央の検知領域へ入ると、左右と下へパネルが展開する。
3. 「画質のみ」「画質＋音質」「音質のみ」の3領域を左から並べる。領域にはアイコンと文字を併記する。
4. ポインタが入った領域を強調し、選択される圧縮方法を明示する。
5. ドロップでキューへ追加する。離脱・ドラッグの取り消しでは閉じる。
6. 処理中はパネルを閉じる。進捗、キャンセル、結果、出力先を開く操作はメニューバーから利用できるようにする。

物理的なノッチ部分に内容を表示することはできないため、「中央」はノッチ直下のパネル領域を含める。ノッチ本体上でドロップを受けられるかは実機で確認し、直下に確実に操作できる領域を設ける。

## 圧縮の意味

| ドロップ位置 | 映像 | 音声 |
| --- | --- | --- |
| 左：画質のみ | 再エンコードして容量を減らす | 元の音声ストリームを保持 |
| 中央：画質＋音質 | 再エンコードして容量を減らす | 再エンコードして容量を減らす |
| 右：音質のみ | 元の映像ストリームを保持 | 再エンコードして容量を減らす |

「保持」は再エンコードしないことを指す。保持したストリームと出力形式の互換性を確認し、対応できない場合は黙って別モードへ変更せず理由を表示する。

音声のみの圧縮は、映像が容量の大半を占める収録では削減量が小さい。音声のないファイルでは右領域を無効にし、理由を表示する。圧縮しても小さくならない場合は結果に明示する。

圧縮強度の数値は未確定。まず代表的な収録で文字の読みやすさ、動き、音声、容量を比較し、既定値を決める。3領域には圧縮対象の選択を担わせ、強度は設定画面に分ける。

## 初期版の安全な保存方針（提案）

- 元ファイルを残し、同じフォルダに別名で保存する。名前が衝突しても上書きしない。
- 一時出力を検証してから完成ファイルとして扱う。
- キャンセル・変換失敗時は元ファイルを保持する。
- 完了時に元サイズ、出力サイズ、削減率を表示する。
- 元ファイルが残る間は空き容量が増えないため、出力と元ファイルをFinderで確認する導線を用意する。自動削除は初期仕様に含めない。
- 複数ファイルは順番に処理する。

## 実装順

1. **操作プロトタイプ**：ノッチ位置への追従、ドラッグ時のみの展開、3領域の選択、離脱時の閉鎖を実機で確認する。
2. **圧縮処理**：動画の事前解析、3モード、キュー、進捗、キャンセル、衝突しない保存、出力検証を実装する。
3. **日常利用**：結果表示、設定、ログイン時起動、複数ディスプレイ、ノッチのない画面を整える。

## 検証する利用場面

- Finderからの実際のファイルドラッグでのみ開き、普通のポインタ移動では開かない。
- 左・中央・右へのドロップが表示どおりの処理になる。
- 処理対象外のストリームが再エンコードされない。
- 音声なし、複数ファイル、非対応ファイル、空き容量不足、キャンセルを扱える。
- 画面の切り替え、メニューバーの自動非表示、フルスクリーン時の干渉を実機確認する。
- 代表的な画面収録で、圧縮後の可読性、再生、長さ、音声を確認する。

現段階は仕様案。アプリの実装・圧縮値の決定・実機での動作検証は未実施。
